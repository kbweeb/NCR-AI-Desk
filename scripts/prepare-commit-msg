#!/bin/sh
# Strip Cursor Agent co-author trailers (Cursor adds these automatically).
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0
grep -vi 'cursoragent@cursor.com' "$MSG_FILE" | grep -vi '^Co-authored-by: Cursor' > "${MSG_FILE}.tmp"
mv "${MSG_FILE}.tmp" "$MSG_FILE"
